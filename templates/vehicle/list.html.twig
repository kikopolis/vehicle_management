{% extends 'base/app.html.twig' %}

{% block body %}
	<div class="container text__white">
		<h1>{{ title }}</h1>
		<div class="vehicle__list--card--grid">
			{% for vehicle in vehicles %}
				{{ include('vehicle/parts/_small_list_card.html.twig') }}
			{% endfor %}
		</div>
	</div>
{% endblock %}

{% block title %}{% if title is defined %}{{ title }}{% else %}{% trans %}vehicle.title.generic{% endtrans %}{% endif %}{% endblock %}